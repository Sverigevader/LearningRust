Det finns ett autoformateringsverktyg som heter rustfmt som finns installerat redan, testa det.

Rust indenterar med 4 mellanslag, inte tab... 
    WOW...

Om man använder en metod och har ! på slutet anropar man ett macro. 
    Mer i kap. 19.

Rust är ett "ahead of time" compiled language;
    ett kompilerat program går att köra utan rust installerat.

cargo new skapar upp ett git repo automatiskt men inte om det redan finns
    går att konfigurera...

Rustprojekt har alltid en src mapp med all kod och alla andra filer bredvid, config cargo.toml osv

continue::link
    https://doc.rust-lang.org/book/ch02-00-guessing-game-tutorial.html

In rust variables are immutable by default.
    let x = 5 // immutable
    let mut x = 25 // mutable

String class is a growable UTF-8 encoded String

::new indicates that new is an asssociated function with previous type

continue::link
    https://doc.rust-lang.org/book/ch02-00-guessing-game-tutorial.html#handling-potential-failure-with-the-result-type

funktioner returnerar antingen OK eller Err (enums) med ett felmeddelande typ som nedan...
    // Följande rad betyder essentiellt att om det inte fungerar; 
    // Prefixa felmeddelande i "Err" med argumentet (strängen) som är given. 
    // Känns som att formuleringen blir lite galen va?
    .expect("Failed to read line!"); 

continue::link
    https://doc.rust-lang.org/book/ch02-00-guessing-game-tutorial.html#comparing-the-guess-to-the-secret-number

